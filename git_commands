## Git Commands

git init - Creates a new local repository with the specified name

git add - snapshots the file in preparation for versioning

git commit - Records file snapshot permanently in version history

git status - lists all new or modified files to be committed

git log - lists version history for a file, includes renames

git push - uploads all local branch commits to GitHub

git pull - downloads bookmark history and incorporates changes